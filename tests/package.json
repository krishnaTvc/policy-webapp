{
  "name": "policy-webapp-tests",
  "version": "1.0.0",
  "description": "Playwright e2e tests for PolicyVault",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
